<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :price %><br />
    <%= @ticket.price %>
  </div>
  <div class="field">
    <%= f.label :screeninginfo_id %><br />
    <%= f.number_field :screeninginfo_id %>
  </div>
  <div class="field">
    <%= f.label :ticketname_id %><br />
    <%= f.collection_select :ticketname, Ticketname.all, :id, :name %> 
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
